React Fonts v 1.1.0
===========

#Usage:#
var Font = require("react-fonts");

##Client:##
<Font fonts={ [{"id": "fonts", "href": "//fonts.googleapis.com/css?family=Source+Serif+Pro:400,600,700|Open+Sans+Condensed:300"}] }/>

##Server side rendering:##

var fonts = Font.rewind();

function getLinkTags(fonts) {
  if(!fonts) return "";
  return fonts.map(function(font, i) {
    return '<link rel="stylesheet" type="text/css" id="' + font.id + '" href="' + font.href + '"/>';
  }).join("");
}

console.log(getLinkTags(fonts));
